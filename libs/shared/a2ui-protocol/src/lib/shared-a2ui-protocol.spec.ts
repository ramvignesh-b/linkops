import { sharedA2uiProtocol } from './shared-a2ui-protocol';

describe('sharedA2uiProtocol', () => {
  it('should work', () => {
    expect(sharedA2uiProtocol()).toEqual('shared-a2ui-protocol');
  });
});
