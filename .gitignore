# Dependencies
node_modules
.pnpm-store

# Nx
.nx/cache
.nx/workspace-data
.nx/migrate-runs

# Build outputs
dist
tmp
out-tsc
bazel-out

# Environment variables & secrets
.env
.env.local
.env.*.local
!.env.example

# Testing & Coverage
coverage
.nyc_output

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Agent working area (issue tracker, maps, plans — scaffolding, not product)
.scratch

# Reference material and personal study notes, not product source
docs/*.pdf
docs/study

# Claude Code: project commands are shared, per-user settings are not
.claude/settings.local.json

# AI agent tooling from `nx configure-ai-agents`. Useful locally for the Nx MCP
# server and its skills; not part of the product, so it stays out of the repo.
# Matched without a trailing slash: in a worktree `.agents` is a symlink to
# the main checkout, and `.agents/` matches only a real directory.
.agents
.gemini/
.claude/settings.json
CLAUDE.md

# IDE & OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.sublime-workspace
*.swp
*.swo

.angular

__screenshots__/

vitest.config.*.timestamp*
.nx/polygraph
.claude/worktrees
